# agent-of-record on the Mac — complete setup and test guide (main c1cb08b, 2026-09-16)

**Supersedes** the 2026-09-15 evening guide (main 73eedf4). Same shape, same test list; the pins, counts and one new component are corrected below.

For Jordan, and for the Claude Cowork session on the Mac that will do the work. Everything that session needs is in this file or in the repository at the commit named below. Nothing here assumes the session has seen the PC conversations.

Basis: the PC clone at `c1cb08b`, which is also `origin/main` there, read on the evening of 2026-09-16 — main moved twice while this guide was being written (the v0.3.5 release, then the MCP Registry entry), so the pins were re-read at 22:20 CDT. GitHub itself was not reachable from the session that wrote this; the release and registry facts below come from the PC's own job reports, and one item (the state of #15) is marked *unconfirmed*.

## A. What changed since the 2026-09-15 guide

| Item | 2026-09-15 guide said | Now |
|---|---|---|
| Commit under test | 73eedf4 | **c1cb08b** (or whatever `git pull` brings; note the hash) |
| Releases | 0.3.2 latest; "cut 0.4.0 tomorrow" | **0.3.3** (2026-09-15), **0.3.4** and **0.3.5** (2026-09-16) are dated CHANGELOG sections; **v0.3.5 is tagged and released** (5 assets); `CITATION.cff` is 0.3.5. An "Unreleased" section already exists at c1cb08b (the MCP Registry entry). |
| Release gate | `RELEASE_CHECK: CLEAN (25 checks)` | **`RELEASE_CHECK: CLEAN (32 checks)`**, 18 self-test suites |
| Skills plugin | `plugin.json` 0.6.0, list at top level | **0.6.1**; the ten-entry list lives under `metadata.skills`; `CoworkConfig/plugin/skills/` is a generated copy the gate checks byte for byte |
| Executor packaging | config-file registration only | **also an MCP Bundle**, `aor-batch-exec-1.3.0.mcpb` — a v0.3.5 release asset (SHA-256 in section 0), built and handshaken, **never installed by a desktop host** |
| Install pages | "not yet operated" → flip after tonight | Already say "executor operated on a Mac 2026-09-15; skills-plugin upload not yet confirmed" (0.3.3). Only the plugin-upload half remains to flip. |
| Job scripts | `CommandJobs/mac-evidence-*.sh` | Moved to `CommandJobs/archive/mac-evidence/` (still runnable by relative path) |
| Lessons pipeline | `lesson_check.py` + `release_check.py` | Two more generated artifacts are gate-checked: the measured table and the plugin `skills/` tree (section 5) |
| Corpus | 120 entries, digest 94 rules | Unchanged: 120 / 94 (`docs/measured-results.md`) |
| Route, skills policy, executor contract, test steps 1–9, third-party inference | — | Unchanged |

## 0. State of the repository tonight

| Item | Value |
|---|---|
| main | **c1cb08b** — https://github.com/jordanmrash/agent-of-record (PC clone and `origin/main` agree; v0.3.5 is tagged one commit back, at 2552c3d) |
| What landed since 73eedf4 | **0.3.3 (2026-09-15)** — the Mac install cells match the evidence; the measured table in `README.md` and `docs/measured-results.md` is generated by `scripts/measured_table.py` and the gate fails when it is stale; `scripts/operator_name_check.py` fails the gate on operating text that names the operator (198 lines rewritten to "the operator"); the four dated one-off jobs and the eleven `mac-evidence-*.sh` scripts moved to `CommandJobs/archive/`; skills plugin 0.6.1. **0.3.4 (2026-09-16)** — `CoworkConfig/plugin/` is a real plugin root with a generated `skills/` tree (`build_plugin.py --tree`, checked by `--check-tree` in the gate); the manifest's shipping list moved to `metadata.skills`; `.claude-plugin/marketplace.json` at the root (Claude Code install path; closes #21); `*.approved` declared LF. **0.3.5 (2026-09-16)** — the executor ships as an MCP Bundle built by `scripts/build_mcpb.py`; `build_mcpb.py --check` joins the gate; `server.json` for the MCP Registry and `glama.json`; `CITATION.cff` 0.3.5. **Unreleased (c1cb08b)** — `server.json` at the root, the MCP Registry entry for the bundle (v0.3.5 asset URL and SHA-256), and `.github/workflows/mcp-registry.yml`, which publishes it on a published release or on demand through GitHub OIDC. |
| Claude route | Unchanged. One MCP server: `aor-batch-exec` (the approved command executor). `aor-filesystem` and `aor-playwright` are retired on this route — Claude Cowork has connected folders and a browser of its own. `install-mac.sh --register` already removed both from `claude_desktop_config.json` on this Mac (49d1fd4). |
| Skills | Unchanged. All ten ship to every product. Five are redundant under Claude Cowork with an Anthropic account and are meant to be **disabled in the host, not deleted**: `persistent-memory`, `local-file-bridge`, `playwright-skill`, `git-bridge`, `skill-menu`. Each says so in a "When this skill is redundant" section; the matrix is `docs/skills-by-configuration.md`. |
| Plugin | `CoworkConfig/plugin/.claude-plugin/plugin.json` **version 0.6.1**, ten entries under `metadata.skills`, no product scoping. `CoworkConfig/plugin/skills/` is a generated copy — never edit it. Build: `python3 scripts/build_plugin.py --strict --platform macos` → `OK 10 skill(s)` → `Outputs/Skills Plugin/agent-of-record-skills-macos.plugin`. (The README also offers the built `.plugin` from the release assets; build locally tonight so the file matches the commit under test.) |
| Executor bundle (new) | `aor-batch-exec-1.3.0.mcpb`. `python3 scripts/build_mcpb.py` builds it to `Outputs/MCP Bundle/` with a SHA-256 sidecar; deterministic zip of `manifest.json`, `server/index.js`, `README.md`, `LICENSE`. Manifest `Startup/CommandBridge/mcpb-manifest.json`: server `node ${__dirname}/server/index.js`; `COWORK_ROOT` from the required **Tooling root** directory setting (default `${HOME}/agent-of-record`); `COWORK_CONFIG_ROOT` from the optional **Cowork config root** setting (empty = no operating-rules reminder); `COWORK_ROUTE=claude`. The packaged copy strips the generated `PLUGIN-LESSONS` block from the tool description; the live reminder read at run time is unchanged. **Status: released as a v0.3.5 asset, SHA-256 `d6dfc6fab9b4926e526d4857232faa59262617de83d20412f43697e67ed7985b`, verified by downloading it back on the PC; built and handshaken on the PC and in CI; no desktop host has installed it; the assumption that the host starts it on the Node runtime it bundles is "to be confirmed by the first real install."** |
| Corpus | `CoworkConfig/cowork-memory/cowork-lessons.md`: 120 entries, 94 rules, 31 always-on (generated table). Lessons may carry `Routes:` / `Platforms:`; the executor filters the operating-rules block by route (`exec-server.sh` sets `COWORK_ROUTE=claude`; the bundle manifest sets the same). |
| Gate | `python3 scripts/release_check.py` → `RELEASE_CHECK: CLEAN (32 checks)`; CI ran both platforms green before each merge. |
| Evidence already on record | `docs/evidence/mac-operated-2026-09-15.md`: `run_batch_file("hello-mac.sh")` ran on this Mac (arm64, macOS 26.6.2) through `aor-batch-exec`; install check, release gate and plugin build also ran on the Mac through the executor. Steps 4–5 of that record (plugin upload, restart, `ListSkills`) were **not** done — that is tonight. `docs/evidence/` holds no later Mac record. |
| Open issue | #15 "executor registered, never connected" — evidence exists; closes when `install-mac.sh --verify` output is posted. *Unconfirmed tonight:* still open as far as the PC shows. |
| Releases on GitHub | **v0.3.5 is released**: tag at 2552c3d, GitHub release with 5 assets including `aor-batch-exec-1.3.0.mcpb` and its `.sha256` (the PC's release job downloaded the bundle back and matched the digest). **MCP Registry publication is not confirmed**: the PC's publish run refused because `mcp-registry.yml` was not yet visible on main; c1cb08b carries it now. For step 11, build the bundle locally or download the v0.3.5 asset — either way its SHA-256 must match the digest above. |
| Tooling root on the Mac | `~/agent-of-record` (not under `~/Documents`, so the desktop app is never granted Documents access). Jobs: `~/agent-of-record/CommandJobs/*.sh`. Outputs: `~/agent-of-record/Outputs/`. Logs: `~/agent-of-record/CommandJobs/Logs/`. Machine settings: `~/agent-of-record/Startup/posix/cowork-env.sh` (gitignored). Desktop app logs: `~/Library/Logs/Claude/mcp.log` and `mcp-server-aor-batch-exec.log`. |

## 1. Before starting Claude — Jordan, in Terminal (five minutes)

```bash
cd ~/agent-of-record
git status --short --branch          # expect clean; untracked scratch (hello-mac.sh, test-executor.sh) is fine
git fetch origin && git checkout main && git pull --ff-only
git log --oneline -1                 # expect c1cb08b — if newer, note the hash: it is the commit under test
python3 scripts/release_check.py     # expect RELEASE_CHECK: CLEAN (32 checks)
python3 scripts/build_plugin.py --strict --platform macos
                                     # expect: 10 skill(s) selected ... OK Outputs/Skills Plugin/agent-of-record-skills-macos.plugin
python3 scripts/build_mcpb.py        # only if step 11 is on tonight: expect aor-batch-exec-1.3.0.mcpb + sidecar in Outputs/MCP Bundle/ (same digest as the v0.3.5 asset)
bash Startup/posix/install-mac.sh --verify
                                     # expect: aor-batch-exec registered; aor-filesystem / aor-playwright absent; save this output for #15
```

If `git pull` complains about local changes to `CommandJobs/*.sh` or `docs/evidence/*`, stash them (`git stash`) — they are last night's scratch, already represented on main. Expect the pull to **move** files: 0.3.3 relocated `mac-evidence-*.sh` to `CommandJobs/archive/mac-evidence/` and the dated one-offs to `CommandJobs/archive/`. `hello-mac.example.sh` is still `Startup/posix/hello-mac.example.sh`; your untracked `CommandJobs/hello-mac.sh` is unaffected.

Then install the plugin in the app, by upload — the file cannot be opened from Finder: **Claude → Customize → Plugins → Add → Upload plugin** → choose `~/agent-of-record/Outputs/Skills Plugin/agent-of-record-skills-macos.plugin`. It should appear under "Created by you". Then quit Claude completely (Cmd+Q) and reopen it — the app reads its MCP configuration and the plugin only at launch.

**Executor registration tonight: keep the config-file registration** (`install-mac.sh --register`, already done). The MCP Bundle is a separate, optional measurement — step 11 — taken only after steps 1–6 pass, so a bundle problem cannot be confused with a route problem.

## 2. In the Claude Cowork session — the test, step by step

Start a Cowork session in the desktop app (a local session; a cloud session cannot reach a local MCP server). Give it this file. (`docs/handoff/aor-handoff-2026-09-15-mac.md` in the repository is the older, superseded handoff — this file replaces it; do not follow its branch, count or release lines.)

| # | Step | Done when |
|---|---|---|
| 1 | `ListSkills` | ten skills: command-bridge, self-improvement, dream-cycle, gamma-tango, not-a-robot, persistent-memory, local-file-bridge, playwright-skill, git-bridge, skill-menu |
| 2 | **+** → Connectors | exactly one: `aor-batch-exec`, one tool `run_batch_file` |
| 3 | Signed in with the Anthropic account: under Customize, disable persistent-memory, local-file-bridge, playwright-skill, git-bridge, skill-menu. Record whether the toggle is per skill or per plugin — that detail is undocumented and goes in the evidence file. | five off, five on |
| 4 | `run_batch_file` with `file = hello-mac.sh` | exit 0; result names `Outputs/Executor Test/result.txt`; the output says `Machine: arm64, macOS ...` (a Linux line means the session shell answered, not the executor) |
| 5 | Read the operating-rules block returned with that job | rules that apply here — nothing about dev tunnels, Ports panel, tasks.json or bridge-health.bat |
| 6 | Have the session run `python3 scripts/install_check.py --route local --json install-results.json` through the executor (write a one-line `.sh` under `CommandJobs`, propose it, approve, run) | `INSTALL_CHECK: CLEAN`; bridges in scope 1 of 4; 10 in the repository, 10 ship to claude. Keep the JSON: it becomes `docs/evidence/install-macos-2026-09-16.json` (the naming `docs/evidence/README.md` gives) |
| 7 | Optional but the most valuable measurement of the night — third-party inference (section 3) | a yes/no on memory, with the model named |
| 8 | Write `docs/evidence/mac-operated-2026-09-16.md` (template in section 4) and add the JSON from step 6; commit on a branch `evidence/mac-2026-09-16`, push, open a PR against main | PR open, CI green |
| 9 | Post the `install-mac.sh --verify` output as a comment on #15 | #15 has it; Jordan closes it |
| 10 | Only after 1–6 pass: in the same PR, change the status lines of `docs/install/claude-cowork-mac.md` and `claude-cowork-mac-quickstart.md` from "skills-plugin upload not yet confirmed" to confirmed, citing the evidence file. Add a bullet naming the evidence to the CHANGELOG "Unreleased" section (it exists at c1cb08b: the MCP Registry entry). While in the long page, fix the four lines that contradict this guide (section 5, "Install-page drift"). | pages updated |
| 11 | **Optional, after 1–6 pass — first real install of the MCP Bundle.** Settings › Extensions › Advanced settings › Install Extension → `~/agent-of-record/Outputs/MCP Bundle/aor-batch-exec-1.3.0.mcpb` (built in section 1, or the v0.3.5 release asset — same digest). Tooling root = `~/agent-of-record`. Config root: `~/agent-of-record/CoworkConfig` to reproduce the step-5 rules block, or leave empty to see the no-corpus behaviour. Quit and reopen Claude. Record: whether the connector list shows one executor or two (the config-file entry is still registered); whether the bundled server found `node` (the open assumption in the 0.3.5 entry; `mcp-server-aor-batch-exec.log` says); `run_batch_file hello-mac.sh` → exit 0 with the `Machine: arm64` line. | a paragraph in the evidence file, whatever happened |

Approval rules for the session: it proposes the exact `.sh` and the output folder under `~/agent-of-record/Outputs/`, Jordan approves once, it writes the job and runs it, then reports stdout, stderr, exit code and the `COWORK_RESULT:` line. Every job ends with exactly one `COWORK_RESULT: OK` or `COWORK_RESULT: FAIL <reason>` and an exit code that agrees.

## 3. Third-party inference — the memory test (step 7)

Claude Desktop → Developer → Configure Third-Party Inference. On macOS, Ollama also offers a one-toggle setup (Ollama app → Apps → Claude → On; pick a model under Settings → Apps). The harness, connected folders, browser, plugins and MCP servers stay; only the model changes. The session is signed out of the Anthropic account while a gateway is active.

Turn it on with a local model (30B-class if the machine has the memory; note the exact model). Start a new Cowork session. Confirm `aor-batch-exec` is still listed and `run_batch_file hello-mac.sh` still returns exit 0 — the route does not depend on the model.

Memory: in session A, state a fact to remember. Start session B. Ask for it.
- Remembered → account memory survives the gateway; `persistent-memory` stays off.
- Not remembered → re-enable `persistent-memory` under Customize; it is needed in this configuration.

Record this in the evidence file — it changes a repository decision.

Note whether Customize → Skills came up empty under the gateway (reports say it can); if so, upload the plugin again and record that too.

Turn the gateway off (Ollama → Claude → Off restores the previous configuration) and restart Claude before continuing with any Anthropic-account steps.

Expect a weaker model to follow the delivered rules less reliably. The enforced controls do not depend on the model: the executor's schema (one file parameter, no arguments, no shell), its refusals, `job_lint`, `lesson_gate`, `release_check`.

## 4. Evidence file template — docs/evidence/mac-operated-2026-09-16.md

```
# macOS operated evidence — 2026-09-16
Device: <hostname>, Apple arm64, macOS <version>. Claude Desktop <version>.
Commit under test: c1cb08b on main (replace if git pull brought a newer main).
Operator route: Claude Cowork, local stdio route, tooling root ~/agent-of-record.
Gate on this machine: RELEASE_CHECK: <paste the line>

## Plugin install
- Plugin version: 0.6.1 (agent-of-record-skills-macos.plugin built at the commit above)
- Upload path used: Customize > Plugins > Add > Upload plugin. Result: <appears under Created by you: yes/no>
- Restart: Cmd+Q, reopen.
- ListSkills: <count> — <names>
- Toggle granularity under Customize: <per skill | per plugin> ; disabled: <list>

## Connectors
- Listed: <names> ; tools: <names>

## run_batch_file hello-mac.sh
<paste the result: exit code, machine line, output path>

## Operating-rules block on the first job
<paste it; note which rules apply to this route>

## install_check --route local (through the executor)
<paste the summary lines; JSON committed as install-macos-2026-09-16.json>

## Third-party inference (if done)
- Gateway/model: <e.g. Ollama qwen3:30b> ; executor still reachable: <yes/no>
- Memory across sessions when signed out: <yes/no> ; persistent-memory re-enabled: <yes/no>
- Skills panel state under gateway: <populated/empty>

## MCP Bundle (if done)
- File: aor-batch-exec-1.3.0.mcpb, SHA-256 <from the sidecar; the v0.3.5 asset is d6dfc6fab9b4926e526d4857232faa59262617de83d20412f43697e67ed7985b>
- Install dialog result: <installed/refused, verbatim>
- Connector list afterwards: <one executor | two — names>
- node found by the bundled server: <yes/no; quote mcp-server-aor-batch-exec.log>
- run_batch_file hello-mac.sh through the bundle: <exit code, machine line>

## What did not work
<anything, verbatim>
```

Rules for the record: state only what was observed, quote error text verbatim, never mark a page "operated" from a check that ran in the session's own shell — that shell is a Linux VM on this Mac, not the machine. Evidence comes through `aor-batch-exec`.

## 5. Facts the session will need

**Git.** main is protected: changes arrive by pull request, two CI gates (windows-latest, macos-latest) must pass, Jordan is the code owner. Never push to main directly — 704071e did that by admin bypass and the rewind was refused. Branch, push, `gh pr create` (or open the PR on github.com if `gh` is not installed). Jordan merges from the PC with the standing merge job or on GitHub.

**The session's shell cannot reach github.com or the npm registry** (its proxy answers 403 — recorded on the install page). Push through an executor job — last night's `CommandJobs/archive/mac-evidence/mac-evidence-push.sh` is the shape — or Jordan pushes from Terminal.

**Executor contract.** `run_batch_file` takes one property, `file`, a path relative to `~/agent-of-record/CommandJobs`, `.sh` only, must already exist. No arguments, interpreter, cwd, env, timeout or elevation can be passed. Start every job with `cd "$COWORK_ROOT"` (the launcher derives it). Declare the output folder with a `# COWORK_OUTPUT: <path>` line under `Outputs/` and write through `$COWORK_JOB_OUTPUT`. Archived scripts run by relative path: `archive/mac-evidence/<name>.sh`.

**Session shell ≠ machine.** `uname -a` in the session says Linux; that is the sandbox. Any claim about the Mac needs the executor's output.

**Retired servers.** If the connector list still shows `aor-filesystem` or `aor-playwright`, the app has not been restarted since `install-mac.sh --register` ran. Cmd+Q and reopen.

**install_check is strict on schema dialect** (2020-12 only). Do not relax it by reasoning; the open question `cowork-draft07-well-formed-untested` is the record.

**Generated artifacts, regenerated never edited.** The release gate at c1cb08b checks four generated things and fails when any is stale: the digest and the `SKILL-LESSONS` blocks (as before); the measured table in `README.md` / `docs/measured-results.md` (`python3 scripts/measured_table.py` rewrites it; `--check` is the gate); the plugin `skills/` tree under `CoworkConfig/plugin/` (`python3 scripts/build_plugin.py --tree` rewrites it; `--check-tree` is the gate); and the `.mcpb` (`build_mcpb.py --check`). A new lesson touches the first three.

**Operator by name.** `scripts/operator_name_check.py` fails the gate on any skill instruction, lesson, digest line, job or server text that addresses the operator by name outside an attribution block. Write "the operator" ("the author" under `docs/`). If the gate names a line, fix the line; do not add an exclusion.

**Lessons.** Log a new lesson in `CoworkConfig/cowork-memory/cowork-lessons.md` in the existing schema (Pattern-Key, Date, Trigger, Rule, Delivered-to, Failed, Why, Worked, Evidence) only for something that will save time next time; run `python3 CoworkConfig/Skills/self-improvement/scripts/lesson_check.py CoworkConfig/cowork-memory/cowork-lessons.md`, regenerate the three artifacts above, then `release_check.py` before committing. Work-specific or personal-task lessons do not belong in this corpus (PR #20 set that rule).

**Skills are never deleted for redundancy.** Disable in the host; re-enable when the configuration changes.

**Do not claim the third-party-inference path is operated on Windows**; the pages say it is written on the assumption Windows reaches parity, and the first real PC call is still owed.

**CHANGELOG.** An "Unreleased" section exists at c1cb08b (the MCP Registry entry); the evidence PR adds its bullet there. Do not renumber or edit the dated 0.3.3–0.3.5 sections.

**Install-page drift (fix in the step-10 PR).** `docs/install/claude-cowork-mac.md` still carries pre-narrowing text that contradicts this guide and its own section 6: "must end with `RELEASE_CHECK: CLEAN (24 checks)`" (section 2) → 32; "five skills" and "confirm `ListSkills` returns all five repository skills" (section 7) → ten; "`bridges in scope: 3 of 4` - the three this route has" (section 9) → 1 of 4; "The browser and filesystem bridges are registered alongside it by `install-mac.sh`" (intro) and the "registered by the installer" filesystem/browser rows (section 3) → retired 2026-09-15, as section 6 already states. The quickstart's status line is current.

## 6. What Jordan does afterwards (PC side)

- Merge the evidence PR; close #15.
- 0.3.5 is released (tag at 2552c3d, 5 assets, bundle digest verified) and `server.json` is on main. Open: the MCP Registry publication — the PC's publish run refused because `mcp-registry.yml` was not yet visible on main; re-run it against c1cb08b, or dispatch the workflow on demand, and record the result on the release.
- Next release — 0.3.6 or 0.4.0, your call — cites tonight's evidence file: CHANGELOG "Unreleased" → dated section, `CITATION.cff` version and date, tag, GitHub release.
- Decide `enforce_admins` on main (currently false: it allowed the direct push and it is also what the `--admin` merge job relies on).
- If the Mac PR did not, fix the install-page drift listed in section 5, and retire or replace `docs/handoff/aor-handoff-2026-09-15-mac.md` with this guide.
- Small follow-ups already identified: make `install_check`'s dialect check route-aware (Copilot accepts draft-07 — measured); tidy the tax-vocabulary examples in not-a-robot, persistent-memory, skill-menu, and the alteryx-* citation in dream-cycle; add a gate that refuses withheld lesson keys in the published corpus.

## 7. Reference

- Repository: https://github.com/jordanmrash/agent-of-record — Releases: https://github.com/jordanmrash/agent-of-record/releases
- Issue #15: https://github.com/jordanmrash/agent-of-record/issues/15
- `CHANGELOG.md` — 0.3.3, 0.3.4, 0.3.5 entries (what changed since 73eedf4, in the project's own words)
- PR #19 https://github.com/jordanmrash/agent-of-record/pull/19 — route narrowing + PC corrections (a88d38c); PR #20 https://github.com/jordanmrash/agent-of-record/pull/20 — corpus scoped (73eedf4). History only.
- `docs/handoff/aor-handoff-2026-09-15-mac.md` — superseded by this file (branch, 25-check and 0.4.0 lines are stale); `docs/handoff/aor-handoff-2026-09-15.md` — the Mac's own handoff, superseded in part
- `docs/skills-by-configuration.md` — which skill to keep on in which configuration
- `docs/install/claude-cowork-mac.md`, `claude-cowork-mac-quickstart.md`, `claude-cowork.md` — install pages
- `docs/evidence/mac-operated-2026-09-15.md` — last night's evidence; `docs/evidence/README.md` — naming for the install JSON; tonight's record goes beside them as `mac-operated-2026-09-16.md`
- `docs/measured-results.md` — the generated figures this guide quotes
- `Startup/CommandBridge/mcpb-manifest.json`, `mcpb-README.md`, `scripts/build_mcpb.py` — the MCP Bundle
- `server.json` (root) and `.github/workflows/mcp-registry.yml` — the MCP Registry entry and the workflow that publishes it (Unreleased at c1cb08b)
